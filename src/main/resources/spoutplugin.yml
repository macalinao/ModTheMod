## ${project.name} plugin file
---
name: ${project.name}
description: ${project.description}
url: ${project.url}
version: ${project.description}
author: THEDevTeam

main: org.thedevteam.modthemod.spout.MTMSpoutPlugin
platform: BOTH
